<%- include('../partials/header') %>
<section>
  <div class="section-header">
    <h2>Certificates & Transcripts</h2>
    <a class="button" href="/admin/certificates/new">Issue Certificate</a>
  </div>
  <table class="table">
    <thead>
      <tr>
        <th>Student</th>
        <th>Program</th>
        <th>Level</th>
        <th>Title</th>
        <th>Issued</th>
        <th>Reference</th>
        <th>PDF</th>
      </tr>
    </thead>
    <tbody>
      <% certificates.forEach(cert => { %>
        <tr>
          <td><%= cert.student_name %></td>
          <td><%= cert.program %></td>
          <td><%= cert.level %></td>
          <td><%= cert.title %></td>
          <td><%= cert.issued_at %></td>
          <td><%= cert.reference %></td>
          <td><a class="link-button" href="/certificates/<%= cert.id %>/pdf">Download</a></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</section>
<%- include('../partials/footer') %>
